# Unified Plotter Requirements
# Core dependencies (Python 3.8+ compatible)
pandas>=1.5.0,<3.0.0
matplotlib>=3.5.0,<4.0.0
numpy>=1.21.0,<2.0.0
Pillow>=9.0.0,<11.0.0
requests>=2.28.0,<3.0.0
psutil>=5.9.0,<6.0.0

# Development dependencies
pytest>=7.0.0,<8.0.0
pytest-cov>=4.0.0,<5.0.0
black>=22.0.0,<24.0.0
flake8>=5.0.0,<7.0.0
mypy>=0.991,<2.0.0

# Build dependencies
pyinstaller>=5.0.0,<6.0.0
cx-freeze>=6.0.0,<7.0.0
setuptools>=65.0.0,<70.0.0
wheel>=0.37.0,<1.0.0

# Note: tkinter is built-in to Python and doesn't need to be installed via pip
